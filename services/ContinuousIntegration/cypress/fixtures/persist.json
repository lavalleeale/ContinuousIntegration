{
    "containers": [
        {
            "id": "persist",
            "image": "base",
            "steps": [
                "node -e \"require('http').createServer(function (req, res) { res.end('OK'); }).listen(process.env.PORT);\""
            ],
            "persist": true
        }
    ]
}