{
    "containers": [
        {
            "id": "persist",
            "image": "base",
            "steps": [
                {
                    "type": "shell",
                    "command": "node -e \"require('http').createServer(function (req, res) { res.end('OK'); }).listen(process.env.PORT);\""
                }
            ],
            "persist": true
        }
    ]
}